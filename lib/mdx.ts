/**
 * MDX Component Map
 * Maps markdown elements to React components for rendering
 */

export const mdxComponents = {
  // These component mappings can be extended for custom markdown rendering
  // For now, we rely on standard HTML rendering
};

export function getMDXComponents() {
  return mdxComponents;
}
